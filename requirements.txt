# OPTIMIZED Insurance Document Query System Requirements (v3.0 - NVIDIA Only)
# Core PDF processing (HYBRID APPROACH - best quality)
pdfplumber>=0.9.0  # For accurate table extraction
PyMuPDF>=1.23.0
pandas>=2.0.0  # For table processing

# Machine Learning and NLP (OPTIMIZED SET)
sentence-transformers>=2.2.0  # For MiniLM cross-encoder (10x faster than BGE)
scikit-learn>=1.3.0  # For TF-IDF hybrid search
# torch>=2.0.0  # Will be installed by sentence-transformers if needed
# transformers>=4.30.0  # Will be installed by sentence-transformers if needed

# NVIDIA Embeddings (PRIMARY - NO FALLBACKS)
requests>=2.28.0  # For NVIDIA NIM API calls

# Advanced Re-ranking Models (BAAI) - Fallback option for reranking only
FlagEmbedding>=1.2.0

# Vector Database and Search
pinecone>=7.0.0

# LLM Integration
google-generativeai>=0.3.0

# Web Interface
streamlit>=1.28.0

# System Monitoring (optional)
psutil>=5.9.0

# Utilities
python-dotenv>=1.0.0

# REMOVED: Heavy LangChain dependencies for semantic chunking
# langchain>=0.1.0                 # REMOVED - too heavy, replaced with SimpleChunker
# langchain-experimental>=0.0.50   # REMOVED - not needed anymore
# langchain-huggingface>=0.0.2     # REMOVED - not needed anymore